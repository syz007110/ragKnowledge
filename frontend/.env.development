# 复制为 .env.development 后按需修改（Vue CLI 在 npm run dev 时自动加载）
# 勿提交含密钥的 .env.development（若仓库忽略规则未覆盖，请自行勿提交）

# 开发服务器监听地址：默认 localhost；需 WireGuard/局域网 IP 访问时改为 0.0.0.0
#DEV_SERVER_HOST=0.0.0.0

# 端口（与 vue.config.js 默认一致；可不写）
#DEV_SERVER_PORT=8081

# 从其它机器浏览器访问本机前端时，填「浏览器能访问到的」后端根地址（可选）
VUE_APP_API_BASE_URL=http://10.88.0.2:3301
